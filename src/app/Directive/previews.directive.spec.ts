import { PreviewsDirective } from './previews.directive';

describe('PreviewsDirective', () => {
  it('should create an instance', () => {
    const directive = new PreviewsDirective();
    expect(directive).toBeTruthy();
  });
});
